/**
 * @param {string} s
 * @return {string}
 */
var longestPalindrome = function (s) {
  let out = [];

  for (let i = 0; i < s.length; i++) {
    const curr = s[i];
  }

  return out.join("");
};
//TODO: finish
console.log(longestPalindrome("babad"));
